/**
 * Auth page.
 *
 * Owns authentication feature orchestration while delegating presentation
 * and business logic to smaller auth components.
 */
export default function AuthPage() {
  return null;
}