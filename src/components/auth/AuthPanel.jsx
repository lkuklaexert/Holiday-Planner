/**
 * Authentication panel.
 *
 * Auth is being extracted from App.jsx so login, reset password and
 * password update flows can be maintained independently from planner logic.
 */
export default function AuthPanel() {
    return null;
  }