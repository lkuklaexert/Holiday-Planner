/**
 * Application shell.
 *
 * This will become the home for global layout, navigation and page routing.
 * For now it safely renders existing application content unchanged.
 */
export default function AppShell({ children }) {
    return children;
  }